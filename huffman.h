#ifndef __HUFFMAN_H
#define __HUFFMAN_H

//need huffman class that have a class to build huffman tree.



//encode huffman tree
int huffman_encode(const char* source,char* dst);
//decode huffman tree
int huffman_decode(const char* source,char* dst);

#endif